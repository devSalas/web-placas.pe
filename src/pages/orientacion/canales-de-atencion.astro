---
import Header from "@components/Header.astro";
import { dataCanalesAtencionPage } from "@data/page/orientacion/canales-de-atencion";
import Layout from "@layouts/Layout.astro";
---

<Layout title="Canales de Atención">
  <Header
    title={dataCanalesAtencionPage.h1}
    subtitle={dataCanalesAtencionPage.subtitle}
  />
  <div
    class="pb-[40px] p-[20px] sm:p-[40px] lg:p-[40px] flex flex-col gap-[20px] sm:flex-row w-full"
  >
    <ul class="list-inside list-disc pl-6 flex flex-col gap-[30px]">
      {
        dataCanalesAtencionPage.content.map((item) => (
          <div class="flex flex-col gap-[10px]">
            <li class="text-lg sm:text-xl lg:text-2xl font-bold">
              {item.title}
            </li>
            <div class="pl-[20px]">
              {item.parrafos.map((text) => (
                <p set:html={text} />
              ))}
            </div>
          </div>
        ))
      }
    </ul>
  </div>
</Layout>
